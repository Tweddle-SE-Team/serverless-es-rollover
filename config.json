{
   "bucket": "backups",
   "clusters": {
      "cluster1": {
         "address": "http://localhost:9200",
         "rollover_conditions": {
            "max_age": "1m"
         },
         "indices_to_keep": 2,
         "exclude_aliases": ["assets", "logs"]
      }
   }
}
